#include <stdio.h>

int run()
{
  fwrite("Good... Wait what?\n", 19, stdout);
  return system("/bin/sh");
}

int main(int argc, const char **argv, const char **envp)
{
  char s[64];

  return (int)gets(s);
}